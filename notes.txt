      <form className={styles.formBar} onSubmit={onFormSubmit}>
        <input
          type="text"
          placeholder="Snack"
          value={title}
          required
          onChange={(e) => setTitle(e.target.value)}
          disabled={loadingAddOrEdit}
        />
        <input
          type="number"
          placeholder="$5.00"
          value={price}
          onChange={(e) => setPrice(e.target.value)}
          disabled={loadingAddOrEdit}
        />
        <input
          type="text"
          placeholder="Location"
          value={location}
          onChange={(e) => setLocation(e.target.value)}
          disabled={loadingAddOrEdit}
        />
        <button type="submit" disabled={loadingAddOrEdit}>
          {loadingAddOrEdit ? "Loading..." : editMode ? "Update" : "Add"}
        </button>
      </form>